spring:
  flyway:
    locations: classpath:db/migration,classpath:db/demo

server:
  servlet:
    session:
      cookie:
        # El entorno local usa HTTP; producción mantiene el valor seguro del perfil base.
        secure: false

fincore:
  identity:
    registration-enabled: true
