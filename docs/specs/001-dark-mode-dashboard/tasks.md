# Tasks – 001 Modo oscuro en la aplicación autenticada

Legend: `[ ]` pending · `[x]` done · `[P]` may run in parallel with other `[P]` tasks (disjoint files, no dependency).
Each task is ≤2 hours, names its files, the test written first, and the AC it satisfies.

- [x] T01 [P] Resolve initial theme from stored preference or OS setting — files: `frontend/src/theme/ThemeContext.ts`, `frontend/src/theme/ThemeProvider.tsx`, `frontend/src/theme/ThemeProvider.test.tsx` — test first: "resolves dark when OS prefers dark and no stored preference" and "resolves light when OS prefers light and no stored preference" (mock `matchMedia`, empty `localStorage`) — AC: AC-1, AC-2
- [x] T02 Toggle theme manually and persist the choice — files: `frontend/src/theme/ThemeContext.ts`, `frontend/src/theme/ThemeProvider.tsx`, `frontend/src/theme/ThemeProvider.test.tsx` (depends on T01) — test first: "toggle switches theme value" and "reads persisted preference on mount, ignoring current OS preference" (seed `localStorage` before mount) — AC: AC-3, AC-4
- [x] T03 [P] Build the `ThemeToggle` UI primitive — files: `frontend/src/components/ui.tsx`, `frontend/src/components/ui.test.tsx` — test first: "renders with role switch/aria-pressed reflecting current theme" and "toggles on click and on Enter/Space" — AC: AC-7
- [x] T04 Wire `ThemeProvider` and `ThemeToggle` into the app shell — files: `frontend/src/main.tsx`, `frontend/src/components/AppShell.tsx` (depends on T01, T02, T03) — test first: extend `frontend/src/App.test.tsx` with "clicking the sidebar theme control switches theme immediately, without navigation or reload" — AC: AC-3
- [x] T05 Verify theme toggle parity in the mobile menu — files: `frontend/src/App.test.tsx` (depends on T04) — test first: "theme toggle is visible and functional when the mobile menu is open" (render at narrow viewport with `sidebar-open` state) — AC: AC-5
- [x] T06 Guard auth pages against theme leakage — files: `frontend/src/App.test.tsx` (depends on T04) — test first: "login page renders with light styling regardless of a stored dark theme preference" (seed `localStorage` dark, render `/login`, assert no dark scope reaches `.auth-*`/`.form-stack` outside `.app-frame`) — AC: protects the spec's non-goal boundary
- [x] T07 Convert `styles.css` to theme-aware color tokens — files: `frontend/src/styles.css` (depends on T04, T06 passing as the regression net) — test first: none new (CSS-only change); rely on T04–T06 plus a manual visual pass over `card`, `metric-*`, `status-badge`, `data-table`, `nav-link` and form controls in both themes — AC: AC-6
- [x] T08 Full local verification — run `pnpm test:frontend`, `pnpm lint:frontend`, `pnpm build:frontend` — AC: all (definition-of-done gate), no file changes

## Verification
- [x] Full test suite passes (`pnpm test:frontend`) — 13/13
- [x] Lint, type check and build pass (`pnpm lint:frontend`, `pnpm build:frontend`)
- [x] `/flow:review` returned APPROVE (per task, T01–T07)
- [x] `/flow:verify` returned ALL_PASS

## Log
<!-- One line per event: date · Tnn · files · tests · reviewed ✓ · verified ✓ · postmortems (cause · fix · prevention). -->
- 2026-09-13 · T01 · `frontend/src/theme/ThemeContext.ts`, `frontend/src/theme/ThemeProvider.tsx`, `frontend/src/theme/ThemeProvider.test.tsx` · 2 new tests, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE; fixed redundant `localStorage.clear()` in test; follow-up noted: `resolveInitialTheme` doesn't guard `localStorage.getItem` against a throw, low risk, no existing precedent — deferred)
- 2026-09-13 · T02 · `frontend/src/theme/ThemeProvider.tsx`, `frontend/src/theme/ThemeProvider.test.tsx` · 3 new tests, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE; fixed: moved `localStorage.setItem` out of the `setTheme` functional updater to keep it pure under StrictMode; follow-up noted: no try/catch around `localStorage.setItem`/`getItem` for quota/private-mode failures, low risk, no existing precedent — deferred, same as T01's nit)
- 2026-09-13 · T03 · `frontend/src/components/ui.tsx`, `frontend/src/components/ui.test.tsx` · 2 new tests, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE; fixed: renamed overclaiming test name to reflect what's actually exercised)
- 2026-09-13 · T04 · `frontend/src/main.tsx`, `frontend/src/components/AppShell.tsx`, `frontend/src/App.test.tsx` · 1 new test + `stubMatchMedia` applied to existing tests, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE; fixed: added `localStorage.clear()` to `App.test.tsx`'s `afterEach` for explicit test isolation; verified `.app-frame`-scoped design constraint followed correctly, no leak into auth pages)
- 2026-09-13 · T05 · `frontend/src/App.test.tsx` · 1 new test, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE, no changes needed; 2 nits noted, non-blocking)
- 2026-09-13 · T06 · `frontend/src/App.test.tsx` · 1 new test, spot-check pass (single test, lint) · Reviewed ✓ 2026-09-13 task scope (APPROVE, no changes needed)
- 2026-09-13 · T07 · `frontend/src/styles.css` · CSS-only (no new tests, per plan), spot-check pass (full suite, lint, build) · orchestrator fix: `.section-heading`/`.activity-item`/`.beneficiary-item` border-bottom still used a hardcoded `rgba(21,60,54,.08-.1)` literal that would render near-invisible on the new dark card background — swapped to `var(--color-border-soft)` which already has a dark counterpart · Reviewed ✓ 2026-09-13 task scope (1st pass: REQUEST_CHANGES — blocker: `.form-stack label` left as a hardcoded literal, illegible on dark backgrounds, affects real dashboard forms in `CustomerPages.tsx`; fixed with a `--color-label-text` token; 2nd pass: APPROVE. Non-blocking note: sidebar/nav-link/avatar intentionally stay visually identical in both themes since they were already dark chrome before this feature — AC-3 lists the sidebar as a place the theme "changes", but there's nothing light to change there; judged acceptable, flagging for visibility)
